# Report generation scripts (generate_report.py, generate_diet_report.py).
# parse_apple_health.py uses only the Python standard library.
python-docx>=1.1.0
matplotlib>=3.8.0
pandas>=2.1.0
numpy>=1.26.0
